name: sankey_flutter_example
description: Example application for the sankey_flutter package.
version: 1.0.0+1
publish_to: none # This is an example app, not a published package

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Local dependency on the parent package
  sankey_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true